language core;

extend with #universal-types;

generic fn test[T](t : T) -> T {
  return (
    generic [T]fn(val : T) {
      return val
    }
  )[T](t)
}

fn main(x : Nat) -> Nat {
  return test[Nat](x)
}
